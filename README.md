# Security Guidance Cold-Start PoC

Owned, harmless two-account reproduction for a Python module search-path
boundary in Anthropic's official `security-guidance` plugin.

The trusted base workflow runs only when the repository owner comments
`@claude-cold-poc` on a pull request. The external fork contributes ordinary
repository content only.
Owned two-account proof for security-guidance cold-start import search path
